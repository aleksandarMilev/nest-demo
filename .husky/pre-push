echo "full lint..."
npm run lint:strict || exit 1

echo "typecheck..."
npm run typecheck || exit 1

echo "build..."
npm run build || exit 1

echo "tests (integration and unit)..."
npm run test:cov -- --bail || exit 1

echo "pre-push checks passed!"
